{
  "schema_version": 3,
  "contract_id": "black-ink-environment-v3",
  "purpose": "Scale premium, recognizable, highly varied coloring-book environments across hundreds of monsters while keeping monster and page JSON small.",
  "identity_test": "If the monster were removed, a reviewer should still identify the kind of place at thumbnail size.",
  "resolved_identity": {
    "spatial_type": "What kind of place/space this is: corridor, crawlway, cavern, crypt, forest clearing, reef, wreck, shrine, etc.",
    "material_language": "The dominant construction/geology/vegetation/materials that make the setting believable.",
    "identity_markers": "One to three unmistakable location cues that prove the setting without clutter.",
    "spatial_read": "The geometry/depth/scale cue that makes the space read correctly around the monster."
  },
  "variation_depth": {
    "registry_path": "data/environment_variation_families.json",
    "required_pools": [
      "geometry_pool",
      "landmark_pool",
      "prop_pool",
      "interaction_pool",
      "anti_repetition_rules"
    ],
    "minimum_options_per_pool": 4,
    "ownership_rule": "Environment-family variation supplies reusable options. The resolved environment profile and explicit page variant remain authoritative.",
    "selection_rule": "Use only family options compatible with the resolved profile. Never force an option that changes the named place.",
    "colorability_rule": "Variation must come from a few large structural choices, not from extra tiny objects or texture.",
    "status": "legacy_compatibility",
    "note": "Retained for backward compatibility and coarse family diversity audits. New generation uses the v3 component_engine and overlay_engine."
  },
  "prompt_rules": [
    "State spatial type, material language, identity markers, and spatial read explicitly.",
    "Use at least one large structural cue and one unmistakable location marker.",
    "The environment must remain identifiable if the monster is mentally removed.",
    "Do not use smooth featureless walls, generic room boxes, or vague scenery when a more specific setting is required.",
    "Environment cues must be large enough to color comfortably and visually secondary to the monster.",
    "At least one environment feature must participate in the story moment.",
    "Use the family variation palette only when compatible with the named profile and explicit page variant.",
    "Vary geometry before adding more props or texture.",
    "Use at most one dominant environmental landmark and one supporting prop unless the page remains highly open.",
    "Negative space is intentional coloring space; do not fill it simply to make the scene busier."
  ],
  "automatic_failures": [
    "setting type cannot be identified at thumbnail size",
    "wall/floor/ceiling treatment is so generic that dungeon, building, cave, or outdoor location is ambiguous",
    "materials do not match the named place",
    "no unmistakable location marker is visible",
    "spatial geometry contradicts the named environment",
    "background could be swapped with an unrelated page without changing the scene",
    "composition repeats the same environment geometry without a page-specific reason",
    "variation adds tiny clutter, texture, or props that make the page less enjoyable to color"
  ],
  "minimal_page_inputs": [
    "environment_profile_id",
    "environment_variant.landmark",
    "environment_variant.framing",
    "environment_variant.interaction"
  ],
  "inheritance_rule": "Environment family owns reusable visual language; profile owns place identity; component catalogs own scalable scenery; overlays own cross-family themes; page recipes own only the unique story landmark/framing/interaction.",
  "scalability_rule": "Creature data declares identity and habitat compatibility. Reusable scenery, construction, terrain, lighting, hazards, props, depth cues, lair dressing, and environmental interactions belong to the universal environment engine.",
  "component_engine": {
    "catalog_dir": "data/environment_components",
    "overlay_path": "data/environment_overlays.json",
    "required_groups": [
      "spatial_archetypes",
      "primary_surfaces",
      "ground_planes",
      "overhead_features",
      "lighting_features",
      "structural_features",
      "landmarks",
      "secondary_features",
      "hazards",
      "depth_features",
      "atmosphere_features",
      "interaction_patterns"
    ],
    "minimum_options_per_group": 6,
    "context_rule": "Component compatibility is resolved from the environment profile and reusable context triggers, not from monster-specific scenery JSON.",
    "deterministic_selection": "A stable page/profile key selects a reproducible palette so reruns can be diagnosed while different pages naturally rotate components.",
    "prompt_budget": "Select a compact compatible palette; never dump the entire library into the image prompt.",
    "colorability_rule": "Every selected component must create large or medium readable coloring regions. Micro-detail is never used as the source of variety.",
    "page_authority": "Explicit page landmark, framing, and interaction remain authoritative and override selected palette suggestions."
  },
  "overlay_engine": {
    "purpose": "Reusable environmental modifiers such as lair, ruin, sacred, military, burial, trap, treasure, fungal, settlement, and weathered can apply across multiple environment families.",
    "inference": "Infer overlays from profile identity, page moment, and explicit environment variant; allow future explicit environment_overlays without requiring them.",
    "ownership_rule": "Overlays add compatible environmental character but never change the base family or named profile."
  },
  "assembly_budget": {
    "core_groups": [
      "spatial_archetypes",
      "primary_surfaces",
      "ground_planes",
      "overhead_features",
      "lighting_features",
      "structural_features",
      "depth_features"
    ],
    "accent_groups": [
      "landmarks",
      "secondary_features",
      "atmosphere_features"
    ],
    "conditional_groups": [
      "hazards"
    ],
    "story_group": "interaction_patterns",
    "major_environment_forms": [
      2,
      4
    ],
    "selected_accents": 2,
    "rule": "The engine may know hundreds of components, but a single page uses only a few strong forms. Library depth creates variety; it must not create clutter."
  }
}
